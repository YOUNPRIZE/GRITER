<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.griter.model.dao.UserDao">

	<insert id="create" parameterType="User">
		INSERT INTO users (user_id, password, email, gender, name, nickname, regist_date, image)
		VALUES(0, #{password}, #{email}, #{gender}, #{name}, #{nickname}, #{regist_date}, #{image})
	</insert>
	
	<select id="selectALl" parameterType="User">
		SELECT * FROM users
	</select>
	
	<select id="selectByNickname" parameterType="String" resultType="User">
		SELECT * FROM users
		WHERE nickname = #{nickname}
	</select>
	
	<update id="update" parameterType="User">
		UPDATE users
		SET password = #{password}, name = #{name}, image = #{image}
		WHERE user_id = #{user_id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM users
		WHERE user_id = #{user_id}
	</delete>
</mapper>